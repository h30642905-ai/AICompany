# AICompany — ゲーム式AIカンパニー

かわいいバーチャルオフィスでAI社員が実際に働くローカルツール。生成はすべて **codex CLI（サブスク・費用0円）**。有料API禁止（CodexAppServer共通ルール準拠）。

## コンセプト

社長（ユーザー）が会社を設立すると、codexがCOO以下6人のAI社員を「採用」。
タスクを振ると社員がcodexで実際に成果物（Markdown）を作り、決裁トレイに提出。
社長は承認/差し戻しをするだけ。会社目標の進捗バーが育つ、経営ゲーム風の実務ツール。

## 機能

1. **会社設立**: 会社名・事業・目標・雰囲気を入力 → codexが社員6人（名前/役職/性格/絵文字/服色）を生成
2. **今日のタスクを決める**: COOが会社目標から今日のタスク3件を提案（担当も適材適所で割り当て）
3. **社員へ仕事を振る**: 指示を書くと社員がcodexで成果物を作成 → 決裁トレイへ
4. **決裁**: 承認で完了（目標+5%）、差し戻しはコメント付きで自動リトライ
5. **COOへ相談**: チャット形式で経営相談
6. **週次レビュー**: 完了タスクからレビューレポートを生成
7. 18時〜6時は自動でナイトオフィス（紫の床）に切り替わる

## デザイン規約（重要）

- **MOTHER2（EarthBound）風レトロRPG**: チェッカー床・黒縁白ウィンドウ・DotGothic16フォント・▶カーソル
- **UIに絵文字は使わない**（翔太さんルール）。キャラ・小物はすべて `public/img/` のドット絵スプライト
  （codex image_gen で生成: 社員10体 + 社長 + 机/会議テーブル/ねこ/観葉植物/ウォーターサーバー。再生成は `sprites-prompt.txt`）
- エモート・通知文はRPG風テキスト（「♪」「カタカタ」「〜した！」）

## 起動

```bash
node bin/cli.js        # サーバー起動 + ブラウザを開く（http://localhost:4680）
# または /aicompany スラッシュコマンド
```

- ポート: 4680（`AICOMPANY_PORT` で変更可）
- **依存ゼロ（npm install 不要）・ビルド不要**。`server.js` / `public/` を直接編集すれば即反映（サーバー再起動のみ）
- codex 呼び出し: `lib/codex.js`（ArtMuseum と同じ単発型パターン）
- モデル/推論強度: `AICOMPANY_MODEL`（既定 gpt-5.5）/ `AICOMPANY_EFFORT`（既定 medium）
- **常時起動について**: サーバーを立ち上げたターミナルが「会社の電源」。閉じると閉店（データは残る）。
  再開はもう一度起動コマンドを実行するだけ。

## データ（`~/.aicompany-data/`）

- `company.json` — 会社と社員（正本）
- `tasks.json` — タスクボード
- `chat.json` — COOとの会話履歴（最新100件）
- `outputs/*.md` — 成果物BOX（社員の納品物・週次レビュー）

## 設計メモ

- codexジョブは**直列キュー**（同時1件）。フロントは3秒ポーリングで `/api/state` を取得
- 社員の見た目は `public/img/chars/char01〜10.png` から設立時にランダム割当（company.json の sprite フィールド）
- 差し戻し時は前回成果物とコメントをプロンプトに含めて再生成

## 修正時の注意

- 依存ゼロ構成。`node --check server.js public/app.js` で構文確認
- 配布は install.sh / install.ps1（GitHub `Getabako/AICompany`、Ashura奥義カード掲載）。
  server.js 等を修正したら GitHub にも push すること（会員のインストーラは GitHub から取得する）
